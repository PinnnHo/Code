name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 */1 * * *'
  workflow_dispatch:

jobs:
  ping_app:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP Request to Streamlit App
        run: |
          curl -I https://testcasegen.streamlit.app/